namespace FlowerSellingWebsite.Models.Enums
{
    public enum BlogStatus
    {
        Draft = 0,      // Bản nháp
        Pending = 1,    // Chờ duyệt
        Published = 2,  // Đã xuất bản
        Rejected = 3    // Bị từ chối
    }
}
